<div class="success-wrapper animate__animated animate__fadeInUp">
    <div class="glass-success shadow-lg">
        <div class="card-body p-4 text-center">

            <div class="icon-circle mb-3">
                <div class="wave"></div>
                <i class="fas fa-check-circle text-white fa-3x"></i>
            </div>

            <h4 class="fw-bold text-white mb-1">Awesome!</h4>
            <p class="text-white-50 mb-4 px-3">
                Your data has been securely saved to the database. Everything looks perfect!
            </p>

            <div class="d-flex justify-content-center">
                <button type="button" class="btn btn-glass-light rounded-pill px-4 shadow-sm" onclick="this.parentElement.parentElement.parentElement.style.display='none'">
                    <i class="fas fa-thumbs-up me-2"></i>Great, Thanks!
                </button>
            </div>
        </div>

        <div class="success-progress"></div>
    </div>
</div>

<style>
    @@keyframes wave-animation {
        0% {
            transform: scale(1);
            opacity: 0.8;
        }

        100% {
            transform: scale(1.6);
            opacity: 0;
        }
    }

    @@keyframes progress-shrink {
        0% {
            width: 100%;
        }

        100% {
            width: 0%;
        }
    }

    .success-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        margin: 20px 0;
    }

    .glass-success {
        background: linear-gradient(135deg, #00b09b 0%, #96c93d 100%);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 25px;
        max-width: 400px;
        width: 100%;
        color: white;
        position: relative;
        overflow: hidden;
    }

    .icon-circle {
        position: relative;
        display: inline-block;
        z-index: 1;
    }

    .wave {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(255, 255, 255, 0.4);
        border-radius: 50%;
        z-index: -1;
        animation: wave-animation 1.5s infinite;
    }

    .btn-glass-light {
        background: rgba(255, 255, 255, 0.2);
        border: 1px solid rgba(255, 255, 255, 0.4);
        color: white;
        transition: all 0.3s;
    }

        .btn-glass-light:hover {
            background: white;
            color: #00b09b;
            transform: translateY(-2px);
        }

    .success-progress {
        height: 5px;
        background: rgba(255, 255, 255, 0.5);
        width: 100%;
        animation: progress-shrink 5s linear forwards;
    }
</style>